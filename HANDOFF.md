# HANDOFF

## Situacao
Base inicial do metodo em construcao.

## Ja feito
- definicao da estrutura principal do repositorio
- definicao das fases macro
- definicao da pasta de legado para futura importacao

## Em andamento
- criacao dos arquivos-base de cada etapa
- definicao dos comandos/agentes/skills do Claude Code

## Proximo passo recomendado
Preencher os arquivos `prompt.md`, `checklist.md` e `saida-esperada.md` de `00-ROTEAMENTO` e `10-DESCOBERTA-E-MODELAGEM` primeiro.

### Excecoes em aberto

Ao retomar o trabalho:

1. Ler a secao "Excecoes recentes" em `STATUS.md`.
2. Ver se existe alguma excecao marcada como "estrutural" sem acao de metodo aplicada.
3. Se houver, priorizar:
   - atualizar `workflow`/`checklists`/`prompts` conforme marcado; e so depois
   - continuar a execucao da demanda principal.
